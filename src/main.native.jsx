// Archived native entry. See `archive/native/main.native.jsx` for original source.
// Native files have been archived because this repository is now web + Node backend.
// (left intentionally empty)
export {};
